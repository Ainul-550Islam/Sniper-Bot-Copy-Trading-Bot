[package]
name = "module-copy"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true

[lib]
name = "module_copy"
path = "src/lib.rs"

[dependencies]
bot-core.workspace = true
solana-kit.workspace = true

solana-sdk.workspace = true

tokio.workspace = true
tracing.workspace = true
chrono.workspace = true
serde.workspace = true
serde_json.workspace = true
# Durable copy journal trait (`recovery::CopyStore`).
async-trait.workspace = true

[dev-dependencies]
tokio.workspace = true
# Integration tests run a mock PumpPortal websocket server.
futures-util.workspace = true
tokio-tungstenite.workspace = true
# Geyser integration tests build realistic wire payloads (encoded
# transactions + status metas).
solana-transaction-status.workspace = true
base64.workspace = true
bincode.workspace = true
tracing-subscriber.workspace = true
# TASK 3 pipeline tests reuse the sniper's mock JSON-RPC node harness
# (`tests/common/mod.rs` includes it by path).
module-sniper.workspace = true
bs58.workspace = true
