sniper-suite 0.1.0 — delivery artifacts note
Produced: 2026-09-18 (packaging sandbox)

ARTIFACTS IN THIS DIRECTORY
  sniper-suite-0.1.0-src.tar.gz   convenience SOURCE SNAPSHOT (secondary artifact)
  SHA256SUMS.txt                  checksum record
  ARTIFACTS-NOTE.txt              this file

SNAPSHOT FACTS
  Files:            170  (frozen software tree 146 @ commit 0e139c3
                          + 23 buyer/delivery docs + scripts/verify-delivery.sh)
  Uncompressed:     3,021,664 bytes
  Archive:          798,886 bytes
  SHA-256:          ca00a02b1689cf6ba5bfd3158f612313a81f51090e03b40c1f80c3fbfb8e6c5f
  Top-level dir:    sniper-suite-0.1.0/
  Integrity checks: verify-delivery.sh 7 PASS / 0 FAIL (run inside the tree
                    before packing); extraction diff -r against the live tree:
                    IDENTICAL (170/170 files); contamination scan (target/,
                    .env, logs, keypairs, dumps): CLEAN.
  Byte integrity:   Rust 2,051,936 B and SQL 23,443 B totals equal the frozen
                    0e139c3 baseline exactly — no code changed in any
                    documentation pass.

WHAT THIS SNAPSHOT IS *NOT*
  It carries NO git history. The authoritative delivery artifact is the
  git bundle produced on the seller's real repository, which must contain:
      9c677cd  (release commit)
      0e139c3  (engineering-freeze commit)
      + the documentation-pass commits
  Procedure and INCLUDE/EXCLUDE specification: docs/ARCHIVE-CHECKLIST.md
  inside the snapshot. Do not represent this tarball as the full delivery;
  it is the convenience snapshot defined in that checklist.

BUYER VERIFICATION (after unpacking)
  cd sniper-suite-0.1.0
  ./scripts/verify-delivery.sh          # 7 PASS / 0 FAIL expected
  cat VERSION                           # 0.1.0
  (full technical acceptance: docs/BUYER-QUICKSTART.md)

NO FABRICATION STATEMENT
  No git metadata existed in the packaging sandbox at production time
  (lost in an infrastructure re-provision); none was recreated. No commit
  hashes, history, or verification results were invented. All test/gate
  numbers quoted in the docs were executed on the identical software bytes
  and are evidenced in AUDIT.md and release-manifest.json.
